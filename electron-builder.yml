appId: us.jettnet.earthy
productName: Earthy
directories:
  output: release
  buildResources: build
files:
  - out/**/*
  - package.json
# GDAL's WASM/data files must live on the real filesystem — emscripten cannot
# read them from inside the asar archive.
asarUnpack:
  - node_modules/gdal3.js/**
extraResources:
  - from: resources/geoid
    to: geoid
mac:
  category: public.app-category.utilities
  icon: build/icon.icns
  target:
    - dmg
  identity: null # no code signing for local builds
win:
  icon: build/icon.ico
  target:
    - nsis
linux:
  icon: build/icon.png
  target:
    - AppImage
fileAssociations:
  - ext: kml
    name: KML Document
    role: Editor
  - ext: kmz
    name: KMZ Document
    role: Editor
